mutation AddProfilePicture($file: Upload!) {
  addProfilePicture(file: $file) {
    errors {
      field
      message
    }
    attachment {
      id
      filename
      mimetype
      encoding
    }
  }
}
