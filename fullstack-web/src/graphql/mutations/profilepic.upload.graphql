mutation AddProfilePicture($file: Upload!) {
  addProfilePicture(file: $file) {
    errors {
      field
      message
    }
    attachment {
      id
      user {
        username
      }
      filename
      mimetype
      encoding
    }
  }
}
