mutation UpdatePost($id: Int!, $input: PostInput!) {
  updatePost(id: $id, input: $input) {
    post {
      id
      title
      text
      updatedAt
      createdAt
      textSnippet
    }
    errors {
      field
      message
    }
  }
}
