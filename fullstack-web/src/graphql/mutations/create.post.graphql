mutation CreatePost($input: PostInput!) {
  createPost(input: $input) {
    post {
      id
      text
      title
      votes
      createdAt
      updatedAt
      creatorId
    }
    errors {
      field
      message
    }
  }
}
